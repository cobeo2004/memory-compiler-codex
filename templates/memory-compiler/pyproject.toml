[project]
name = "memory-compiler-starter"
version = "0.1.0"
description = "Starter knowledge base for memory-compiler-codex"
requires-python = ">=3.12"
dependencies = []

[tool.ruff]
line-length = 100
target-version = "py312"
